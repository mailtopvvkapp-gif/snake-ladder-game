name: Build Android APK

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build-apk:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Set up Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Package Web App into Capacitor
        run: |
          mkdir -p www
          # Copy HTML, JS, CSS, and asset folders into www
          cp -r index.html www/ 2>/dev/null || true
          cp -r *.js *.css assets images style www/ 2>/dev/null || true

          # Initialize npm & Capacitor dependencies
          npm init -y
          npm install --save @capacitor/core@latest @capacitor/cli@latest @capacitor/android@latest

          # Create capacitor configuration file
          cat << 'EOF' > capacitor.config.json
          {
            "appId": "com.game.snakesladders",
            "appName": "SnakesAndLadders",
            "webDir": "www"
          }
          EOF

          # Add and sync android platform
          npx cap add android
          npx cap sync android

      - name: Build Android Debug APK
        working-directory: ./android
        run: |
          chmod +x ./gradlew
          ./gradlew assembleDebug --no-daemon --stacktrace

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Snakes-and-Ladders-APK
          path: android/app/build/outputs/apk/debug/*.apk
          retention-days: 7
          
